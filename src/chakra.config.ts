import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'Roboto', sans-serif`,
    body: `'Lexend', sans-serif`,
  },
  // colors: { red: { 300: "#F43249" } },
  components: {},
});

export default theme;
